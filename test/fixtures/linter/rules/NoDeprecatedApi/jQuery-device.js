sap.ui.define(["jquery.sap.mobile"], function(jQuery) {
	var device = jQuery.device; // jQuery.device is deprecated
	var os = jQuery.os; // jQuery.os is deprecated
	var osVersion = jQuery.os.version; // jQuery.os.version is deprecated
});
