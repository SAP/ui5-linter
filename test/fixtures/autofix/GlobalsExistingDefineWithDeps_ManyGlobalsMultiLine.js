sap.ui.define([
	"sap/m/Dialog"
], function(
	Dialog
) {
	const dialog = new Dialog();
	const combobox = new sap.m.ComboBox();
	const input = new sap.m.Input();
	const label = new sap.m.Label();
	const list = new sap.m.List();
	const panel = new sap.m.Panel();
	const popover = new sap.m.Popover();
	const select = new sap.m.Select();
	const table = new sap.m.Table();
	const text = new sap.m.Text();
	const title = new sap.m.Title();
	const uploadCollection = new sap.m.UploadCollection();
	const viewSettingsDialog = new sap.m.ViewSettingsDialog();
	const wizard = new sap.m.Wizard();
});
