sap.ui.define([], function() {

	// Definitions of jQuery.sap.* API should be ignored

	jQuery.sap.assert = function() {};

	jQuery.sap.isEqualNode = function() {};

	jQuery.sap.ControlEvents = [];

});
